# Nick Docker App

Simple Python application containerized with Docker.

## Run Locally

docker build -t nick-app .
docker run nick-app

## Output

Hello from Nick's first Docker app!
This is running inside a container.
